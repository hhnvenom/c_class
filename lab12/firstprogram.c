#include <stdio.h>

// func to add 2 numbers
int add2Number(int a, int b)
{
    return a + b;
}

int main()
{
    int result = add2Number(3, 5);
    printf("Tong 2 so la: %d\n", result);
    return 0;
}
